const arr = [1, 2, [1, 2], [3, 4, [1, 2, 3]]];

// create a function that sums all the numbers in the array
// sum(arr) => 19 

// create a function that sums all the numbers in the array. dont use semicolon, prefer var instead of let and const

// create 3 functions to sum this array? One should be the most performant, the second should use only modern javascript, the third one should be the easiest to read.